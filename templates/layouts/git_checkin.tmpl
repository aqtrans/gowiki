{{ define "title" }}<title>{{.page.SiteName}}|Check In Files?</title>{{ end }}
{{ define "content" }}
<article>
  <header>{{.page.SiteName}}</header>
  <main>
    <p>The following files were found to be modified outside of Git.</p>
    <p>Would you like to check them in now?</p>
    {{.GitFiles | safeHTML}}
  <form action="/gitadd" method="post">
    <input type="hidden" name="token" value="{{.Token}}">
    <button type="submit">Submit</button>
  </form>
  </main>
</article>
{{ end }}