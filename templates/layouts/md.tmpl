{{ define "title" }}<title>{{ .PageTitle }} - {{.PageTitle }}</title>{{ end }}
{{ define "sidebar" }}
    {{ template "sidebar_nav" . }}
    {{ template "sidebar_cats" . }}
{{ end }}
{{ define "content" }}     
<h1>{{ .PageTitle }}</h1>
{{$title := .PageTitle}}
<p>{{ .Frontmatter.Tags }}</p>
<p>{{ .Frontmatter.Created }}</p>
<p>{{ .Frontmatter.LastModTime }}</p>
<hr />
<div>
{{.Wiki.Content | safeHTML}}
</div>
{{ end }}
